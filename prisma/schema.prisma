// prisma/schema.prisma
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider     = "postgresql"
  url          = env("DATABASE_URL")
  relationMode = "prisma"
}

model User {
  id              String           @id @default(cuid())
  email           String           @unique @db.VarChar(255)
  name            String?          @db.VarChar(255)
  password        String?          @db.VarChar(255)
  emailVerified   DateTime?
  image           String?          @db.VarChar(500)
  createdAt       DateTime         @default(now())
  updatedAt       DateTime         @updatedAt
  lastLoginAt     DateTime?
  preferences     Json?
  role            UserRole         @default(USER)
  status          UserStatus       @default(ACTIVE)
  accounts        Account[]
  files           File[]
  knowledges      Knowledge[]
  projectMembers  ProjectMember[]
  sessions        Session[]
  conversations   Conversation[]
  projects        Project[]        @relation("ProjectOwner")
  projectComments ProjectComment[]

  @@index([email])
  @@index([status])
  @@index([createdAt])
  @@map("users")
}

model Account {
  id                String  @id @default(cuid())
  userId            String
  type              String  @db.VarChar(50)
  provider          String  @db.VarChar(50)
  providerAccountId String  @db.VarChar(255)
  refresh_token     String?
  access_token      String?
  expires_at        Int?
  token_type        String? @db.VarChar(50)
  scope             String? @db.VarChar(255)
  id_token          String?
  session_state     String? @db.VarChar(500)
  user              User    @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([provider, providerAccountId])
  @@index([userId])
  @@index([provider])
}

model Session {
  id           String   @id @default(cuid())
  sessionToken String   @unique @db.VarChar(500)
  userId       String
  expires      DateTime
  user         User     @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@index([userId])
  @@index([expires])
  @@index([sessionToken])
}

model VerificationToken {
  identifier String   @db.VarChar(255)
  token      String   @unique @db.VarChar(500)
  expires    DateTime

  @@unique([identifier, token])
  @@index([expires])
}

model File {
  id        String   @id @default(cuid())
  userId    String
  filename  String   @db.VarChar(255)
  key       String   @db.VarChar(500)
  size      Int
  mimeType  String   @db.VarChar(100)
  url       String   @db.VarChar(500)
  createdAt DateTime @default(now())
  user      User     @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@index([userId])
  @@index([createdAt])
  @@index([mimeType])
}

model Knowledge {
  id                String    @id @default(cuid())
  title             String?   @db.VarChar(500)
  content           String
  category          String    @db.VarChar(100)
  tags              String    @db.VarChar(500)
  source            String    @db.VarChar(255)
  userId            String
  createdAt         DateTime  @default(now())
  updatedAt         DateTime  @updatedAt
  metadata          Json?
  user              User      @relation(fields: [userId], references: [id], onDelete: Cascade)
  generatedProjects Project[] @relation("KnowledgeGeneratedProjects")

  @@index([userId])
  @@index([category])
  @@index([createdAt])
  @@index([tags])
  @@index([title, content, tags])
}

model Conversation {
  id        String   @id @default(cuid())
  title     String   @db.VarChar(500)
  messages  Json
  userId    String
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  metadata  Json?
  user      User     @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@index([userId])
  @@index([createdAt])
  @@index([updatedAt])
  @@map("conversations")
}

model Project {
  id                String           @id @default(cuid())
  title             String           @db.VarChar(500)
  description       String?
  content           String?
  
  // 🔧 更新核心字段
  projectType       ProjectType      @default(DRAFT_PROJECT)
  status            ProjectStatus    @default(IN_REVIEW)
  
  // 🔧 新增AI格式化字段
  aiFormattedContent String?
  formattingStatus   FormattingStatus @default(NOT_STARTED)
  formattingTemplate String?
  formattingHistory  Json?
  
  // 🔧 新增评审流程字段
  currentReviewRound Int             @default(1)
  maxReviewRounds    Int             @default(3)
  allowPublicComments Boolean        @default(true)
  
  // 现有其他字段
  ownerId           String
  visibility        ProjectVisibility @default(PUBLIC)
  knowledgeSourceId String?
  metadata          Json?
  createdAt         DateTime          @default(now())
  updatedAt         DateTime          @updatedAt
  
  // 关系保持不变
  projectMembers    ProjectMember[]
  knowledgeSource   Knowledge?        @relation("KnowledgeGeneratedProjects", fields: [knowledgeSourceId], references: [id])
  owner             User              @relation("ProjectOwner", fields: [ownerId], references: [id], onDelete: Cascade)
  projectComments   ProjectComment[]

  @@index([ownerId])
  @@index([projectType])
  @@index([status])
  @@index([visibility])
  @@index([knowledgeSourceId])
  @@index([createdAt])
  @@index([title, description])
  @@map("projects")
}

model ProjectMember {
  id        String     @id @default(cuid())
  projectId String
  userId    String
  joinedAt  DateTime   @default(now())
  updatedAt DateTime   @updatedAt
  metadata  Json?
  role      MemberRole @default(MEMBER)
  project   Project    @relation(fields: [projectId], references: [id], onDelete: Cascade)
  user      User       @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([projectId, userId])
  @@index([projectId])
  @@index([userId])
  @@index([role])
  @@index([joinedAt])
}

model ProjectComment {
  id        String           @id @default(cuid())
  projectId String
  userId    String
  content   String
  parentId  String?
  createdAt DateTime         @default(now())
  updatedAt DateTime         @updatedAt
  metadata  Json?
  status    CommentStatus    @default(ACTIVE)
  project   Project          @relation(fields: [projectId], references: [id], onDelete: Cascade)
  user      User             @relation(fields: [userId], references: [id], onDelete: Cascade)
  parent    ProjectComment?  @relation("CommentReplies", fields: [parentId], references: [id], onDelete: Restrict, onUpdate: Restrict)
  replies   ProjectComment[] @relation("CommentReplies")

  @@index([projectId])
  @@index([userId])
  @@index([parentId])
  @@index([status])
  @@index([createdAt])
  @@map("project_comments")
}

model SystemLog {
  id        String   @id @default(cuid())
  level     String   @db.VarChar(20)
  message   String
  context   Json?
  timestamp DateTime @default(now())
  userId    String?

  @@index([level])
  @@index([timestamp])
  @@index([userId])
}

model ApiRequest {
  id         String   @id @default(cuid())
  method     String   @db.VarChar(10)
  path       String   @db.VarChar(500)
  statusCode Int
  userId     String?
  duration   Int
  userAgent  String?  @db.VarChar(500)
  ipAddress  String?  @db.VarChar(45)
  timestamp  DateTime @default(now())

  @@index([method])
  @@index([path])
  @@index([statusCode])
  @@index([userId])
  @@index([timestamp])
}

// 🔧 更新枚举定义
enum UserRole {
  USER
  ADMIN
  MODERATOR
}

enum UserStatus {
  ACTIVE
  INACTIVE
  SUSPENDED
}

// 🔧 重新设计项目类型枚举
enum ProjectType {
  DRAFT_PROJECT
  STANDARD_PROJECT
  TEAM_PROJECT
  RESEARCH_PROJECT
}

// 🔧 重新设计状态枚举，支持完整流转
enum ProjectStatus {
  DRAFT
  IN_REVIEW
  FINALIZING
  RECRUITING
  IN_PROGRESS
  COMPLETED
  ARCHIVED
  FAILED
}

enum ProjectVisibility {
  PRIVATE
  TEAM
  PUBLIC
}

enum MemberRole {
  OWNER
  ADMIN
  MEMBER
  VIEWER
}

enum CommentStatus {
  ACTIVE
  DELETED
  HIDDEN
}

// 🔧 新增AI格式化状态
enum FormattingStatus {
  NOT_STARTED
  PROCESSING
  COMPLETED
  FAILED
}